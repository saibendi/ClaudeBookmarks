# Log

Running log of changes/decisions to this project, newest first — for picking back up later.

## 2026-08-06

**Changed:**
- Fixed a real flaw in the `added_at` recency field caught before it caused visible damage: `fetch_tiktoks.py` was stamping wall-clock fetch time, which works for `fetch_bookmarks.py`'s live weekly poll but not for TikTok's one-time backfill through `urls.md` — batches run in whatever order they happen to, not in the file's own rough chronological structure (oldest sections at top, newest "2026" section at bottom). Caught in practice: the "2026" section was fetched first as the initial test batch, so those 8 items — the newest content in the whole backlog — got the *earliest* `added_at` of the entire eventual backfill; every subsequent (older) batch would get a later stamp, inverting the real chronology exactly where it matters.
- Fix: stopped stamping `added_at` for TikToks entirely — they now rely on the existing `added_at or created_at` fallback, same as tweets that predate the field. Simpler than deriving a synthetic recency signal from `urls.md` position (the more "correct" fix, needs an interpolation scheme, still wouldn't be exact); per-item accuracy loss (bookmark time vs. post time can differ) is an acceptable tradeoff for not having batch-order artifacts silently invert the sort.
- Retroactively stripped `added_at` from the 8 already-fetched TikToks in both `data_tiktok.js` and their copies in `categories.js`, re-sorted. Verified: Fashion drawer's two TikToks now rank by their real `created_at` (Dec 30-31, 2025) among the surrounding tweets instead of being artificially pinned above everything.
- `fetch_bookmarks.py`'s `added_at` stamping is untouched — that use case doesn't have this problem.

## 2026-08-05

**Changed:**
- Created `categorize_tiktoks.py` (project root, not `TikTokDownloader/` — it needs sibling access to `data.js`/`data_tiktok.js`/`categories.js`, all of which live at the root) — Stage 2 of the TikTok integration. Mirrors `categorize_bookmarks.py`'s reconcile pattern exactly (same prompt structure, same Claude Haiku model, same batch/backup/merge logic) but reads both `data.js` and `data_tiktok.js`, flattens both into one list, and writes into the same shared `categories.js`.
- Ran it for real: 307 items already categorized (across 14 existing categories) + 8 new TikToks → reconcile mode correctly classified only the 8 new ones, reused all 14 existing category names (no new categories invented), backed up the old `categories.js` to `categories_old.js` first. Classifications landed sensibly (Star Wars content → Movies, interior-design TikTok → Architecture & Interior Design, two outfit-post TikToks → Fashion, etc.).
- Verified in `categories.html`: both Fashion-category TikToks render correctly (author, handle, date, caption, stats, "View Original →"). Confirmed the already-known Stage 3 limitation in the same view — `categories.html`'s hard 2-image cap (`tweet.images.slice(0, 2)`) means only 2 of each photo post's 6 images show, not a new bug.

- Stage 3: fixed `categories.html`'s hard 2-image cap (`tweet.images.slice(0, 2)`) and upgraded `day.html`'s wrapping image grid, per Decision #4. Both now branch on image count: ≤2 images keep the exact previous side-by-side layout unchanged (zero risk to how normal 1-2 image tweets already looked); >2 images (TikTok photo posts, and incidentally any 3-4 image tweet that was silently losing images too) switch to single-image-at-a-time paging — prev/next buttons (hover-revealed), an always-visible "N / total" counter, and a hover-to-unmute `<audio>` element for `audio_url` (photo posts' background music), mirroring the existing video hover-for-audio convention exactly.
  - Added `.tweet-media-nav`/`.tweet-media-counter` and `.cat-card-media-nav`/`.cat-card-media-counter` CSS (absolute-positioned overlay, opacity-0-until-hover for the nav buttons, matching each page's existing `*-video-hint` styling).
  - Note: the `Edit` tool repeatedly failed to match `day.html`'s existing block despite visually-identical `old_string` (likely an invisible-character mismatch tied to its `\uXXXX` escape sequences) — worked around by patching the file directly via a Python script instead.
  - Verified live in both pages: `day.html`'s 15-image post correctly pages one image at a time with a working "1 / 15" → "2 / 15" counter; `categories.html`'s two 6-image Fashion posts page correctly too, confirmed by directly dispatching click events on the nav buttons (coordinate-based clicks were unreliable against the cabinet's 3D `rotateY` transform — `getBoundingClientRect()` returns an axis-aligned box that doesn't match the visually rotated hit-area, so clicks need to target the actual element via `find`/`dispatchEvent`, not raw screen coordinates, when the cabinet is involved). Also hit two same-session browser-cache artifacts unrelated to the actual code (stale cached `style.css` needing a hard refresh; images briefly painting as blank beige boxes for a frame after each page-turn, confirmed harmless via `img.complete`/`naturalWidth`).

- Switched category-card sort from `like_count` descending to recency descending (closes `featuresToImplement.md` item 1). Real motivation: with TikTok merged in, `like_count` sort was actively broken for mixed categories — TikTok engagement runs 10-100x higher than typical tweet likes, so tweets would almost always sink below TikToks regardless of when either was added. Added a new `added_at` field — stamped with wall-clock time when an item is first fetched into the system, distinct from `created_at` (when the content was originally posted on its source platform) — since neither Twitter's bookmarks API nor TikTok's metadata exposes an actual "when you bookmarked/favorited this" timestamp.
  - `fetch_tiktoks.py`: stamps `added_at` in `build_item()` at real fetch time, going forward.
  - `fetch_bookmarks.py`: `merge_tweets_by_date()` now stamps `added_at` only the first time a tweet is ever seen, and carries the original value forward across re-fetches (so a like-count refresh doesn't reset it).
  - `categorize_bookmarks.py`/`categorize_tiktoks.py`: card sort key changed to `added_at or created_at` descending.
  - `categories.html`: same fallback (`recencyKey()` helper) applied to the two places that were also sorting by `like_count` client-side — drawer-merge and card-refile-via-drag insert position — so manual reorganization stays consistent with the new convention.
  - Existing tweets (everything already in `data.js`/`categories.js` before this) intentionally get no backfill — by design, they fall back to `created_at`, which is a close-enough proxy since they were mostly bookmarked near when posted. The 8 already-fetched TikToks got a real one-time backfill instead (`added_at` set to 2026-08-04, matching when that batch actually ran) since the user wanted those treated as "real" additions, not `created_at` fallback.
  - Hit one snag applying this to existing data: `categories.js` stores its own copy of each item, made at classification time — backfilling `data_tiktok.js` alone didn't propagate to the 2 already-classified TikTok copies sitting in `categories.js`, so those needed a separate direct patch. Verified live in `categories.html`: the Fashion drawer now shows both TikToks first (both `added_at` 2026-08-04), tweets following in `created_at` order below them.
  - Removed the now-done item from `featuresToImplement.md`.
- Fixed a real CSS bug in `categories.html`: `.cat-card-text` had `overflow-y: auto` but no `max-height` to ever trigger it, so a long caption just grew to its full natural height inside the flex column and ate the space `.cat-card-images` (`flex: 1; min-height: 0`) needed — squeezing/cropping the media, exactly as reported. Added `max-height: 4.5rem` (~3-4 lines) + `flex-shrink: 0` so text is now reliably capped and scrolls internally instead. Verified against the exact post that surfaced it (`trendsofthetimes`'s 15-image, long-caption post): text height now pins at 72px regardless of caption length, media gets ~187px of healthy space, image renders at full size.
- Made `categories.html` cards bigger per feedback: `.cat-panel` width 380px → 480px, `.cat-card` height `(100vh - 8rem) / 2` → `/ 1.6` (fewer, larger cards per screen). Also fixed a related gap in the same feedback — the new 4.5rem text cap applied even to text-only cards (no image/video), needlessly truncating captions that had the whole pane free to use since nothing else was competing for space. Added a `hasMedia` check in the card-building JS: media-less cards get `.cat-card-text--full` instead (`max-height: none; flex: 1`), letting them use the full available height. Verified live: card height went from ~336-386px to 434px, panel to 480px wide, and a text-only AI & Technology post now shows its complete caption instead of being cut at 4 lines.
  - Bumped further on follow-up feedback ("even bigger"): width 480px → 600px, height divisor 1.6 → 1.3. Verified no horizontal overflow at 1512px viewport (panel right edge 1246px, well within bounds) and card height now 534px.
- Media was still visually cropped even with bigger cards — root cause is `.cat-card-media-wrapper img/video`'s `object-fit: cover`, which always crops to fill the frame regardless of the source's aspect ratio (most acute for TikTok's portrait 9:16 content in a wider frame). Considered true zoom/pan but flagged a real conflict — `.cat-card` is `draggable="true"` for the existing card-refile drag-and-drop, so a drag-to-pan gesture on the image would need careful event handling to not fight with that; a lightbox would sidestep it entirely but is more work. Went with the cheap first move instead: double-click toggles `.cat-card-media-wrapper--contain`, switching that one wrapper's media between `cover` (default, fills frame) and `contain` (whole image visible, letterboxed with the wrapper's own background color). `cursor: zoom-in`/`zoom-out` signals the affordance without adding visible UI chrome. Verified live on a portrait video that was cropped on both sides — double-click now shows the full frame with clean letterboxing.
- Added click-to-pause/resume on videos in both `categories.html` and `day.html` — plain `.play()`/`.pause()` toggle, so resuming is free (native `<video>` never resets `currentTime` on pause). `categories.html` needed a wrinkle `day.html` didn't yet: its wrapper already has a `dblclick` handler (the cover/contain toggle above), and a double-click always fires two `click` events before the `dblclick` — so a naive click handler would spuriously toggle play/pause twice on every double-click. Debounced with a 250ms `setTimeout`, cleared if a second click lands within the window, so double-clicks skip the pause toggle entirely and fall through to the wrapper's `dblclick`. Verified both independently: single clicks correctly flip paused state in both files; a simulated real double-click sequence (click, click, dblclick) left play state untouched while still toggling contain mode.
- Brought `day.html` up to parity with `categories.html`'s cover/contain toggle (was only built for the categories view originally; day.html's media had the identical `object-fit: cover` cropping issue with no fix). Added the same `dblclick` → `.tweet-image-wrapper--contain` pattern and matching CSS. Since day.html's video click-to-pause handler (added just before this) was still non-debounced, retrofitted it with the same 250ms debounce so it doesn't conflict with the new dblclick — same fix `categories.html` already needed. Verified live: single click still pauses/resumes correctly, double-click sequence leaves play state untouched while toggling to the full un-cropped view with letterboxing.

**Decided (no file changes):**
- Remaining TikTok integration work: Stage 4 cosmetic `<title>` tags (may already be fine — checked `categories.html`, already reads "Bookmarks — Categories" not "Twitter Bookmarks", worth a quick pass across the other pages before considering this stage necessary); Stage 5 ongoing backfill (8 of 561 TikToks downloaded so far).

## 2026-08-04

**Changed:**
- Fixed `index.html`: was hardcoded to `const YEAR = 2026` with no way to navigate to any other year (`changeMonth()` clamped month to `[0,11]`, never touched year). Now `currentYear`/`currentMonth` are mutable and `changeMonth()` rolls over across year boundaries. Initial view simplified to always default to today's real year/month. Also added a second pair of nav buttons (`«`/`»`) calling `changeYear(delta)`, so you can jump a full year while staying on the same month instead of clicking through all twelve. Verified via temporary local HTTP server + browser automation: month rollover (Aug 2026 → Jan 2026 → Dec 2025 → back to Jan 2026) and year-jump (3 clicks of `«` from Aug 2026 → Aug 2023 exactly) both correct. Was a blocker for the TikTok backfill (`urls.md` goes back to ~2022-2023) — those years would otherwise have been undownloadable-through-the-UI even once in `data_tiktok.js`.
- Created `TikTokDownloader/fetch_tiktoks.py` — first real implementation of the TikTok pipeline (see `TikTokDownloader/Plan/plan.md` Decisions #1–#7). Downloads via `gallery-dl` only, normalizes output into the flat `media/tiktok/<id>/` layout, fetches video thumbnails as an extra step from the metadata's `originCover` URL, shapes items into the same field names `data.js` already uses, applies the `tt_` id prefix only at write time, merges into `data_tiktok.js` the same read-existing → merge → write way `fetch_bookmarks.py` does for `data.js`. Caught and fixed a path bug before any real run: the script's first draft derived `media/`/`data_tiktok.js` locations from its own folder (`TikTokDownloader/`), one level too deep — `index.html` etc. live at the project root and resolve every path against their own location, same as `data.js`, so both now derive from `PROJECT_ROOT` (the script's parent dir) instead; only `urls.md` stays resolved against the script's own folder. Added `--start-line`/`--end-line` to target a specific `urls.md` section instead of always starting from the top.
- Live-tested `fetch_tiktoks.py` against the 8 URLs in `urls.md`'s "2026" section (`--start-line 578`): all 8 succeeded (4 video, 4 photo), correct folder layout, correct `tt_`-prefixed ids, correct date bucketing by post creation date (not the "2026" bookmark-time label — actual posts spanned 2025-12-30 to 2026-01-06).
- Wired `data_tiktok.js` into the frontend (Stage 1 of the TikTok integration): `index.html`, `day.html`, `stats.html` now load `data_tiktok.js` alongside `data.js` and merge `TIKTOK_DATES`/`TIKTOK_VIDEOS` into the same per-date structures tweets already use — calendar tile counts/badges, day-page item list, and week-stats charts are all now combined tweets+TikToks, not separate views. Also fixed `index.html`'s `day_tweet_snapshot` save to include TikTok ids so day.html's new-item tinting doesn't permanently think every TikTok is unseen. `categories.html` untouched in this pass — it only reads `categories.js`, so it needs `categorize_tiktoks.py` (not yet written) before TikTok items can reach it at all.
  - Verified via temporary local HTTP server + browser automation: Jan 1/3/5/6 2026 calendar badges match `data_tiktok.js` exactly; `day.html?date=2026-01-01` shows both a video and a 15-image photo post (photo grid renders all images without crashing, just visually cramped — expected, matches the still-outstanding Stage 3 slideshow/audio work); `stats.html?weekStart=2025-12-28` daily bar chart and best-of-week both correctly reflect merged data.
  - Confirmed two known, pre-existing gaps surfaced by the test, not introduced by this change: `"View on X →"` link text is wrong for TikTok items (hardcoded copy, Stage 3 item), and `stats.html`'s best-of-week author line shows `@@handle` (double `@`) because its template hardcodes a literal `@` on top of `author_handle`, which already includes one — same bug would affect tweets too, not TikTok-specific.
- Cleaned up `TikTokDownloader/`: removed ad-hoc leftover content from earlier manual tool testing (loose tiger `.mp4`/`.m4a`, `gallery-dl/tiktok/cecireadsstein` folder) via `git rm` — nothing had been committed yet, so no history lost. Deduped `urls.md` (removed one exact-duplicate line, 561 unique TikToks remain).

- Fixed the "View on X →" mislabel (wrong for TikTok links, pointed out right after Stage 1 testing) — changed to platform-neutral "View Original →" in `day.html`, `categories.html`, and `stats.html`'s best-of-week block. Simpler than per-platform detection since it works for both sources as-is.
- Diagnosed and fixed a real playback bug: TikTok videos showed only the poster thumbnail with audio playing but no picture on hover. Root cause — gallery-dl's TikTok extractor just grabs the highest-bitrate URL from TikTok's `bitrateInfo` with no codec preference, and TikTok's CDN is currently serving that as HEVC (confirmed via raw `hvc1` fourcc bytes in the downloaded files — gallery-dl's own `codecType` metadata field falsely claimed "h264"). Chrome/Firefox generally ship no HEVC decoder at all (`canPlayType` returned empty), so the AAC audio track plays but the video track never decodes (`videoWidth`/`videoHeight` stuck at 0) — Safari on macOS wasn't affected (native hardware HEVC support), but fixing at the source removes the browser-dependency entirely. Ruled out yt-dlp as an alternative — it hit the identical HEVC stream for the same test video back when we first compared the two tools.
  - Installed `ffmpeg`/`ffprobe` via Homebrew. Added `video_codec()`/`transcode_to_h264()`/`ensure_h264()` to `fetch_tiktoks.py` — probes the actual codec after download and only re-encodes (`libx264`/`aac`, `+faststart`) when it isn't already H.264, wired into the main fetch loop so every future video gets checked automatically.
  - Added `--reencode-existing` maintenance flag to fix already-downloaded videos in place without re-downloading. Ran it against the 4 existing videos — all 4 were HEVC, all 4 successfully re-encoded, verified via raw fourcc bytes (`avc1` now) and an isolated cache-busted `<video>` element (`videoWidth: 1080, videoHeight: 1920`, decodes cleanly). Note for real-world verification: Chrome's HTTP disk cache can keep serving the old broken bytes for a URL it already loaded once — a hard refresh (Cmd+Shift+R) or new tab clears it.

**Decided (no file changes):**
- TikTok backfill processed in manual batches of ~10-20/day, not one bulk run — lower rate-limit risk, edge cases surface incrementally. New TikToks get added to `urls.md` by hand going forward; no better capture mechanism being built right now.
- Remaining TikTok integration work staged: (2) `categorize_tiktoks.py` so TikTok items reach `categories.js`/`categories.html`; (3) rendering fixes — `categories.html` line ~214 hard-caps every card to 2 images (`tweet.images.slice(0, 2)`), a real bug for TikTok photo posts with 6-15 images, plus the manual slideshow control and `<audio>` element from Decision #4; (4) cosmetic `<title>` tags still say "Twitter Bookmarks".

## 2026-07-29

**Changed:**
- Rotated `ANTHROPIC_API_KEY` in `.env` — old key was returning `401 authentication_error` from `categorize_bookmarks.py`.
- Created `featuresToImplement.md` — backlog, not yet implemented:
  1. Category drawer should sort newest → oldest instead of most-liked → least-liked.
  2. `fetch_bookmarks.py`'s merge is purely additive — never removes a tweet from `data.js` even if unbookmarked on Twitter. Decided: only worth reconciling *within* the fetch window (outside-window removals can't be detected without a full `--all` resync, and aren't a realistic scenario here). Running with `--days 30` weekly covers realistic unbookmark timing once this is built.
- Created `update_bookmarks.py` — runs `fetch_bookmarks.py` then `categorize_bookmarks.py` back to back (so one command instead of two). Defaults to `--days 30`; also supports `--all` and `--reclassify`. Stops immediately if the fetch step fails — does not hang, does not run categorize on a failed fetch.

**Decided (no file changes):**
- `categorize_bookmarks.py` reconcile mode never touches already-categorized tweets; it can create a new category for new tweets, but only if none of the existing ones fit — and it always prints new categories explicitly rather than doing it silently.
- `stats.html`/`categories.html` are static — they only reflect a `categorize_bookmarks.py` run after the page is reloaded, not live.

## 2026-07-28

**Changed:**
- Fixed the core bug in `fetch_bookmarks.py`: `write_data_js` was fully overwriting `data.js` on every run instead of merging. Added `load_existing_data()` + `merge_tweets_by_date()` (same read-existing → reconcile → write pattern `categorize_bookmarks.py` already used for `categories.js`). Verified via isolated unit test + a live end-to-end run: 215 previously-stored bookmarks + 75 new = 290 total after merge, nothing lost.
- Diagnosed why `python3 fetch_bookmarks.py` suddenly failed with `ModuleNotFoundError: No module named 'requests'`: earlier Homebrew installs this session (`gallery-dl` → `pipx` → rust/deno chain) installed `python@3.14`, which took over the `python3` name on `$PATH` ahead of Apple's CLT Python (`/usr/bin/python3`) — the one that actually had `requests`/`python-dotenv`/`anthropic` already installed from back in February. Fix: run scripts with `/usr/bin/python3` explicitly.
- (Also created a project-local `venv/` as an alternative fix, with `requests`/`python-dotenv`/`anthropic` installed — currently unused in favor of `/usr/bin/python3`, but available.)

**TikTok pipeline planning** (see `/Users/saibendi/Documents/TikTokDownloader/Plan/plan.md`):
- Transcribed 6 handwritten planning pages (`IMG_4874`–`IMG_4879`).
- Design decision: TikTok media stored as local downloaded files (yt-dlp → gallery-dl fallback), not remote CDN links — TikTok's raw media URLs are signed and expire, unlike Twitter's stable `pbs.twimg.com`.
- Architecture review of ClaudeBookmarks (confirmed Twitter-only today) + 3 diagrams (setup, data refresh, viewing) added to `plan.md`.
- Key integration finding: `data.js` should NOT be shared/written by both `fetch_bookmarks.py` and a future `fetch_tiktoks.py` (overwrite-collision risk, since fixed for the Twitter side but still relevant to the design) — plan is a separate `data_tiktok.js`, merged with `data.js` at the HTML/render layer; `categories.js` can stay shared since its reconcile pattern is already collision-safe.
- Confirmed X API bookmarks access: 180 req/15min rate limit (not a real constraint), actual constraint is per-read cost (~$0.001/read under the new pay-per-use pricing as of Feb 2026); current account has $5 credit ≈ 5,000 reads.
